# Sound Assets

`videoplayback.m4a` is the primary OTP alarm sound used by `background/offscreen.js`.

`alarm.wav` remains as a simple placeholder asset. If Chrome cannot play the M4A file for any reason, the offscreen alarm player falls back to a generated Web Audio beep.
